const fs = require('fs');
const input = fs.readFileSync('/dev/stdin').toString().trim().split("\n");
const N = input.shift();
const v = input.pop();
const result = input[0].split(" ").map(Number).filter(item => item === +v).length;

console.log(result);
