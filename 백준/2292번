const fs = require('fs');
const input = fs.readFileSync('/dev/stdin').toString().trim();

let count = 1; block = 1;

while(block < input) {
    block += 6 * count;

    count++;
}

console.log(count);
