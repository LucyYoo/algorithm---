const fs = require('fs');

const input = fs.readFileSync("/dev/stdin").toString().trim().split(" ").map(Number);

let answer = input.reduce((acc, cur) => {
  return acc * cur;
})

console.log(answer)
