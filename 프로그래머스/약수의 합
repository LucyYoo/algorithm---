function solution(n) {
    const result = [];
    for(let i = 0; i<=n; i++){
        if(n % i === 0) result.push(i);
    }
    
    const sum = result.reduce((prev, curr) => {
        return prev+curr
    }, 0);
    
    return sum;
}
