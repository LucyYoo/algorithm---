//처음에는 무조건 1번 점프를 한다. (사용량 +1)
//최대한 절반으로 줄였을 때의 값에 어떻게 도달하느냐로 확인
//N을 2로 나눌 수 있으면 순간이동을 사용할 수 있고 없으면 점프를 해야 하는 것
//5000 -> 2500 -> 1250 -> 624 + 1 -> 312 ...
//반복문을 통해서
//n % 2의 나머지가 0이 아닐 경우 answer에 1을 더하고 n -1을 2로 나눈다.
//나머지가 0인 경우 순간이동을 한 것이기 때문에 n / 2만 해준다.

function solution(n) {
    let answer = 1;
    while(n > 1) {
        if(n % 2 !== 0) {
          answer++
          n = (n - 1)/ 2;  
        } else {
            n = n / 2
        };
    }
    return answer;
}
